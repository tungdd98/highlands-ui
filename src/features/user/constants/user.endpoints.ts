export enum UserEndpointsEnum {
  GET_ALL = "/users",
  FIND_BY_ID = "/users/:userId",
  CREATE = "/users",
  UPDATE = "/users/:userId",
  DELETE = "/users/:userId",
  GET_TOTAL_USERS = "/users/total_users",
}
