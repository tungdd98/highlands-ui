export enum DeliveryEndpointsEnum {
  GET_ALL = "/deliveries",
  FIND_BY_ID = "/deliveries/:deliveryId",
  CREATE = "/deliveries",
  UPDATE = "/deliveries/:deliveryId",
  DELETE = "/deliveries/:deliveryId",
  ALL = "/deliveries/all",
}
